{
    "name": "shared",
    "type": "module",
    "scripts": {
        "build": "del-cli build && tsc -p tsconfig.json && tsc-alias -p tsconfig.json"
    },
    "dependencies": {
        "@lesnoypudge/types-utils-base": "^3.4.0",
        "@lesnoypudge/utils": "^14.0.0",
        "chat-app-v4": "file:../.."
    }
}
